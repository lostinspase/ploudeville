all:
  hosts:
    ploudeville-rpi-prod-1:
      ansible_host: 10.0.100.167
      ansible_user: pi
      ansible_port: 22
  children:
    webservers:
      hosts:
        ploudeville-rpi-prod-1:
    dnsservers:
      hosts:
        ploudeville-rpi-prod-1:
