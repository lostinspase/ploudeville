---
- name: Restart ploudeville
  ansible.builtin.systemd:
    name: "{{ systemd_unit_name }}"
    state: restarted
    daemon_reload: true

- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
